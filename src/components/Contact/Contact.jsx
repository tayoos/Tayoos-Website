import React from 'react';
import Button from '../Button/Button'; // Import the Button component

function Contact() {
    const handleClick = () => {
        alert('Button clicked!');
    };

    return <div>{/* Basic Button */}</div>;
}

export default Contact;
